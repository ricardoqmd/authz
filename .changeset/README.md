# Changesets

This folder holds the changesets for this monorepo. Each changeset is a small
markdown file describing which packages changed and how, so that versions and
changelogs are derived from intent rather than written by hand.

Run `pnpm changeset` to add one. See the
[Changesets documentation](https://github.com/changesets/changesets) for details.
